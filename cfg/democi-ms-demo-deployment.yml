apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: ms-ne-democi-demo
  namespace: demo
spec:
  replicas: 2
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ms-ne-democi-demo
    spec:
      containers:
        - name: ms-ne-democi-demo
          image: acrgenesis.azurecr.io/ms-ne-democi-demo:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
           - name: ENV
             value: ambiente
      imagePullSecrets:
        - name: acr-registry
---
apiVersion: v1
kind: Service
metadata:
  name: ms-ne-democi-demo-svc
  namespace: genesis
  labels:
    app: "ms-ne-democi-demo"
spec:
  ports:
    - port: 4041
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ms-ne-democi-demo

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ms-ne-democi-demo-ingress-nginx
  namespace: genesis
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-headers: "request-id, request-date, content-type"
    nginx.ingress.kubernetes.io/cors-allow-methods: "PUT, GET, POST, DELETE, PATCH, OPTIONS"
    cert-manager.io/cluster-issuer: letsencrypt-staging
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "Ingress: $ingress_name";
spec:
  tls:
    - hosts:
        - aks-democi-ingress-cert.eastus2.cloudapp.azure.com
      secretName: ms-ne-democi-demo-ingress-secret
  rules:
    - host: aks-democi-ingress-cert.eastus2.cloudapp.azure.com
      http:
        paths:
          - backend:
              serviceName: ms-ne-democi-demo-svc
              servicePort: 4041
            path: /democi/demo/(.*)